# 개발 일지

## 📅 2026-05-02 개발 내용

### ✅ 완료된 작업

- **보조강사 / 교구 동적 추가 기능 구현 (전체 시스템)**
    - 보조강사를 복수 입력할 수 있도록 `[+ 보조강사 추가]` 동적 UI 도입 (최대 인원 제한 없음, 실사용 5명 이내)
    - 교구를 종류+수량 세트 단위로 복수 추가할 수 있도록 `[+ 교구 추가]` 동적 UI 도입
    - 각 동적 항목에 `[×]` 삭제 버튼 포함, 점선 테두리의 추가 버튼과 빨간 원형 삭제 버튼 디자인 적용
- **Firestore 스키마 변경 (배열 구조 전환)**
    - `subInstructor` (단일 문자열) → `subInstructors` (문자열 배열)로 변경
    - `equipType` + `equipCount` (단일값) → `equipments` (객체 배열 `[{type, count}]`)로 변경
    - 기존 단일값 데이터를 배열로 자동 변환하는 방어 함수(`normalizeSubInstructors`, `normalizeEquipments`) 추가
- **전체 페이지 동적 UI 일괄 적용 (11개 파일 수정)**
    - `index.html` 모달, `input.html` 입력 폼에 동적 추가/삭제 UI 적용
    - `calendar-main.js`: 캘린더 월간 뷰에서 `첫 번째 보조강사 +N명` 축약 표시, 모달 클릭 시 전체 목록 복원
    - `api-handler.js`, `input-handler.js`: 배열 데이터 수집 로직으로 교체
    - `manage-handler.js`: 강사 목록 추출 시 보조강사 배열 순회, 필터링 로직 배열 대응
    - `school-manage-handler.js`: 이력 표시 시 보조강사 `join(', ')` 처리
    - `migrate.js`: 마이그레이션 스키마를 배열 구조로 업데이트
- **동적 입력 필드 디자인 통일**
    - `forms.css`, `input-form.css`의 셀렉터를 `.dynamic-row input`, `.dynamic-list-group label`로 확장하여 기존 폼과 동일한 스타일 적용
- **일정 추가 페이지 스크롤 문제 해결**
    - 보조강사/교구 다수 추가 시 비고란이 화면 밖으로 밀리는 현상 → `form-card`에 `max-height: calc(100vh - 100px)` 및 `overflow-y: auto` 적용
- **Firebase Authentication(이메일 로그인) 전면 도입**
    - `auth_handler.js`: 메인 페이지(`index.html`)용 로그인/로그아웃 상태 관리 및 프리미엄 로그인 UI 연동
    - `auth-check.js`: 서브 페이지(`pages/`) 접근 시 비로그인 사용자 리다이렉트 보안 로직 적용
    - `login.css`: 모던한 글래스모피즘 스타일의 로그인 화면 디자인 적용
    - 인증 상태에 따른 캘린더 렌더링 레이아웃 붕괴 문제(Flexbox) 해결 및 초기화 로직 최적화 (`window.initCalendar`)

### 🐛 해결 중인 버그

- (현재 없음)

### 💡 아이디어 / 다음 할 일

- 데이터 입력 편의성 개선: 출강 이력 입력 시 '학교관리' 데이터를 기반으로 한 자동 완성(Autocomplete) 기능
- 통계 데이터 시각화: 대시보드 구축 및 누적 출강료 확인

---

## 📅 2026-05-01 개발 내용

### ✅ 완료된 작업

- **GAS → Firebase Firestore 마이그레이션 스크립트 작성 (`migrate.js`)**
    - `firebase-admin` SDK 기반의 Node.js 마이그레이션 스크립트 작성
    - JSON 데이터의 한글 키값을 Firestore 영문 카멜케이스 스키마(`date`, `startTime`, `schoolName` 등)로 자동 매핑
    - 500개 단위 `batch` 처리로 대량 데이터 효율적 업로드 (schools 461건, schedules 128건 성공)
    - 숫자 데이터(`대상인원`, `교구수량`)는 `Number()` 형변환하여 Firestore에 정확한 타입으로 저장
- **API 핸들러 Firebase 전환 (`api-handler.js`)**
    - 기존 `fetch(GAS_URL, ...)` 로직을 완전히 제거하고 `db_service.js`의 CRUD 함수 직접 호출로 교체
    - `async/await` 패턴 적용 및 `type="module"` 모듈화 완료
- **캘린더 Firebase 연동 (`calendar-main.js`)**
    - FullCalendar의 `eventSources`에서 `db_service.getSchedulesByDate`를 호출하여 뷰 기간에 맞는 Firestore 데이터를 실시간 조회
    - Firestore 필드명(`date`, `startTime` 등) → FullCalendar 규격(`start`, `end`, `title` 등)으로 변환
- **일정 추가 페이지 Firebase 연동 (`input-handler.js`)**
    - `addSchedule()` 함수를 import하여 Firestore에 직접 일정을 등록하도록 전환
    - 기존 GAS 방식의 `fetch` + `formData` 완전 제거
- **강사 관리 / 학교 관리 페이지 Firebase 전환**
    - `manage-handler.js`: `getAllSchedules()` 사용으로 전환, 전체 일정에서 강사 목록 추출
    - `school-manage-handler.js`: `getSchools()` + `getAllSchedules()` 사용으로 전환
    - `db_service.js`에 `getAllSchedules()` 함수 신규 추가
- **GAS 레거시 코드 전면 정리**
    - `config.js` 파일 삭제 (GAS_URL만 담고 있던 파일, 더 이상 참조 없음)
    - `utils.js`에서 `fetchAndCache()` 함수 제거 (GAS 전용 캐싱 로직)
    - `index.html`, `input.html`, `manage.html`, `school-manage.html`에서 `config.js` 스크립트 로드 제거
    - 모든 핸들러 스크립트에 `type="module"` 적용 완료
    - 프로젝트 전체에서 `GAS_URL` 참조 0건, `fetchAndCache` 참조 0건 검증 완료

### 🐛 해결 중인 버그

- (현재 없음)

### 💡 아이디어 / 다음 할 일

- 보조강사를 복수 인원으로 유동적 추가할 수 있는 기능 구현
- 교구 종류도 유동적으로 추가할 수 있도록 스키마 및 UI 개편

---

## 📅 2026-04-30 개발 내용

### ✅ 완료된 작업

- Firebase 연동 환경 구성 (`firebase_config.js` 생성 및 초기화)
- 브라우저 모듈 환경에 맞춰 CDN 방식의 Firebase v9 Modular SDK 적용
- Firestore 연결 권한 수정 및 연동 테스트(Security Rules) 완료
- 데이터 접근 계층(DAL)인 `db_service.js` 구현 (일정 및 학교 데이터 CRUD 로직 작성)

### 🐛 해결 중인 버그

- (현재 없음)

### 💡 아이디어 / 다음 할 일

- 기존 구글 스프레드시트(GAS)를 통한 데이터 통신을 새로 작성한 Firebase `db_service.js`로 교체(마이그레이션)하는 작업 진행

---

## 📅 2026-04-06 개발 내용

### ✅ 완료된 작업

- **캘린더 목록 뷰(List View) 전면 개편**
    - 캘린더 일정 내역을 테이블 형태의 한 줄 출력 방식으로 배치 (기관명, 프로그램명, 학년/인원, 강사명, 교구, 비고)
    - 월간 보기와 동일하게 사용자가 지정한 색상을 배경색 및 좌측 포인트 컬러 띠로 연동
    - '강사 미정' 붉은 하이라이트 유지 및 '학년', '인원' 누락 시 빈 괄호가 남지 않도록 로직 방어
- **일정 상세/수정 모달 레이아웃 고도화**
    - 모달 창 넘침 및 짤림 방지를 위한 **20열 그리드 기반 4줄 레이아웃 시스템** 최적화 적용
    - 색상, 날짜, 시간, 인원 등 데이터 특성에 맞춰 항목별 너비 균등 및 비율 분할 정렬
    - 직관성을 해치는 중복 색상 옵션(빨강(검토)) 모든 입력 화면에서 깔끔히 제거
- **API 신규 파라미터 연동 극대화 ('학년', '대상인원')**
    - 프론트엔드 모달 데이터를 GAS 서버로 온전히 넘기도록 통신 페이로드(`api-handler.js`) 업데이트
    - 신규 로직 반영 시 브라우저가 과거 코드를 로드하는 캐싱 에러를 파악 후 `?v=2` 파라미터 적용(캐시 바이패스)으로 완벽 해결
- **캘린더 네비게이션 UI/UX 프리미엄 디자인**
    - 캘린더 상단의 년/월 텍스트 크기를 2배(2.8rem)로 확장하여 가독성과 캘린더의 시인성 확보
    - 컨트롤 버튼 그룹을 메인 테마 색상(Blue) 기반의 모던한 라운드 솔리드 버튼으로 재설계
    - 버튼 호버(Hover) 시 부드러운 입체감과 떠오르는 모션 디테일을 추가해 만족도 부여

### 🐛 해결 중인 버그

- (현재 대기 중인 치명적 버그 없음)

### 💡 아이디어 / 다음 할 일

- 데이터 입력 편의성 개선: 출강 이력 입력 시 '학교관리' 시트 데이터를 기반으로 한 자동 완성(Autocomplete) 기능
- 통계 데이터 시각화: 대시보드 구축 및 누적 출강료 확인
- 학교관리 시트의 조회된 데이터를 사용자 친화적으로 직접 수정할 수 있는 기능 추가

---

## 📅 2026-04-02 개발 내용

### ✅ 완료된 작업

- **하이브리드 캐싱(Stale-while-revalidate)을 통한 시스템 전반의 브라우저 로딩 속도 극대화**
    - `sessionStorage`를 활용한 페이지 간 데이터 저장소(Cache) 공유 설계로 **학교관리, 정산관리, 캘린더 등 주요 메뉴 이동 딜레이 0초 달성**
    - 캘린더 페이지 데이터 리로드 방식 개선으로 월간 이동 시 버퍼링 등 딜레이 현상 완벽 제거
- **학교 관리 페이지(`school-manage.html`) UI 및 화면 레이아웃 대대적 개편**
    - 상단 검색 영역과 학교 정보 카드를 상하 배치에서 **좌우 2단 배치(Flexbox)로 변경**하여 출강 이력을 표시할 수 있는 세로 공간을 2배 이상 확보
    - 페이지 전체 컨테이너 가로 사이즈를 화면 너비의 80%(최대 1500px)로 넓혀 모니터 해상도 최적화
    - 왼쪽 검색 박스와 오른쪽 학교 정보 카드의 배경 디자인(그라데이션 및 테두리 컬러) 및 좌우 높이를 완벽하게 일치시킴
    - 정보 카드 영역의 내부 비율을 최적화(좌측 기본 정보 60%, 우측 비고 40%), 특히 내용이 긴 비고 텍스트에 독립 스크롤을 부여
    - 출강 이력 목록 테이블에 세로 스크롤(`overflow-y: auto`) 및 헤더 고정(`position: sticky`)을 적용해 화면 짤림 방지

### 🐛 해결 중인 버그

- (현재 특이사항 없음)

### 💡 아이디어 / 다음 할 일

- 데이터 입력 편의성 개선: 출강 이력 입력 시 '학교관리' 시트 데이터를 기반으로 한 자동 완성(Autocomplete) 또는 드롭다운 선택 기능 검토
- 통계 데이터 시각화: 학교별 총 출강 횟수 및 누적 강사료 요약 정보 표시
- '학교관리' 시트에서 검색용 약칭 일괄 정리 작업 프로세스 수립
- 학교관리 페이지에서 조회된 학교 데이터를 사용자 친화적으로 직접 수정 (삭제는 불가) 할 수 있는 기능 추가

---

## 📅 2026-04-01 개발 내용

### ✅ 완료된 작업

- 학교 관리 페이지(school-manage.html) 신규 구축
    - '학교관리' 시트 데이터 연동: 학교별 상세 정보(주소, 담당자, 연락처 등) 관리 기능
    - 지능형 매칭 로직(Regex) 도입: `기관명 (학교명)` 형식에서 괄호 안의 약칭을 추출하여 정식 학교 정보와 자동 매칭
    - 통합 네비게이션 업데이트: 모든 페이지 헤더에 '학교 관리' 버튼 추가 및 스타일링
    - 기간별 출강 이력 조회: 특정 학교의 과거 모든 수업 기록을 날짜 범위별로 필터링 및 리스트업

### 🐛 해결 중인 버그

- 학교관리 페이지에 스크롤이 없어서 전체 출강 내역을 보기 어려움
- 학교관리 페이지에서 오류 발생
    - 데이터를 불러오는 중 오류가 발생했습니다. 인터넷 연결이나 GAS 설정을 확인하세요.
    - script.googleusercontent.com/macros/echo?user_content_key=... (중략)
    - school-manage-handler.js:34 Data Load Error: SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON

### 💡 아이디어 / 다음 할 일

- 데이터 입력 편의성 개선: 출강 이력 입력 시 '학교관리' 시트 데이터를 기반으로 한 자동 완성(Autocomplete) 또는 드롭다운 선택 기능 검토
- 통계 기능 확장: 학교별 총 출강 횟수 및 누적 강사료 요약 정보 표시
- 관리 페이지에서 데이터를 불러오는 속도 개선
    - 정산관리 페이지는 강사데이터 불러오는데 10 초 가량 소요됨
    - 학교관리 페이지는 학교데이터 불러오는데 70 초 이상 소요됨
- '학교관리' 시트에서 검색용 약칭 정리
- 학교관리 페이지에서 조회된 학교 데이터를 수정 가능하도록 (삭제는 웹에서는 절대 안되도록)

---

## 📅 2026-03-30 개발 내용

### ✅ 완료된 작업

- Antigravity 활용 (매우 좋음)
    - cursor 는 무료 크레딧 한도가 적음
    - VS Code 에서 continue 확장 설치 후 AI 등록하려고 했는데 설정이 안됨
- 전체 파일 검토 후 불필요한 코드 정리
- 달력에 공휴일 표시
    - 공공데이터포탈에서 API를 활용해서 GAS에서 가지고 옴
    - 공휴일 데이터를 활용해서 달력에 빨간색 글자로 표시
    - 공휴일 API 연결이 안돼서 하드코딩으로 수정 (calendar-main.js 파일에 공휴일을 직접 입력)

### 🐛 해결 중인 버그



### 💡 아이디어 / 다음 할 일

---

## 📅 2026-03-27 개발 내용

### ✅ 완료된 작업

- cursor 사용 시작 (매우 만족)
- 캘린더 페이지 모달창 디자인 수정
    - 항목 잘리는 부분 수정
    - 모달창 전체 크기 수정
    - 모달창 내부 항목명 시인성 개선
- 캘린더 디자인 수정
    - 일정 표시 형식 수정 (총 수업시간 표시 : 1.5)
    - 일정 표시 색상 수정 (배경만 색상 들어가도록)
- 일정 추가 페이지 디자인 수정
    - 일정 수정 모달창과 같은 항목 배치
    - 디자인은 개성있게 수정
- 정산 관리 페이지 디자인 수정
    - 전체 디자인을 일정 추가 페이지와 비슷하게
    - 입력 항목을 한 줄에 배치
- 정산 관리 페이지 저장하기(PDF) 설정 수정
- 캘린더 일정 복제 버튼 추가
    - 일정 수정에서 내용 수정하고 복제 버튼 누르면 바로 추가 저장됨

### 🐛 해결 중인 버그



### 💡 아이디어 / 다음 할 일

---

## 📅 2026-03-26 개발 내용

### ✅ 완료된 작업

- 캘린더 페이지 모달창 디자인 수정
    - 전체 크기를 가로 직사각형으로 길게
    - 입력 항목은 4 줄 배치
    - 입력란 표시 항목 전부 Bold 처리 (날짜, 시간)
    - 시간 입력부분을 00:00 형식으로 수정
- 지역 구분 추가
    - 모든 페이지에 지역 구분 추가

### 🐛 해결 중인 버그

- 지역 구분 추가 후 입력창이 일부 잘림
    - 계속 수정해도 안되고 있음 (현재 상황에서 새로 배치를 설명할 것)

### 💡 아이디어 / 다음 할 일

- 일정 추가 페이지(input.html) 레이아웃 구상
- 캘린더에 내용 추가 (시작시간 옆에 괄호로 수업시간 추가 (종료-시작), 보조 강사가 있다면 주강사(보조강사)로 표시)

---

## 📅 2026-03-24 개발 내용

### ✅ 완료된 작업

- 코드 규칙 재정의
    - 기존 코드가 작성되는 방식을 수정 (editModal → edit-modal (kebab-case 방식))
    - 핵심 요소 (ID, edit-modal 등 주요 키워드) 및 디자인 설정을 기록한 'style-guide.md' 파일 생성

### 🐛 해결 중인 버그

- 달력에서 일정 선택 후 수정 적용이 안됨
    - 함수 이름과 데이터 키가 불일치해서 데이터 업데이트가 안되었음
    - api-handler.js 파일 내용을 전체 교체
    - GAS 에서 오류 수정 (추가로 doGet, doPost 에서 유동적으로 시트 데이터를 가지고 오도록 함)

### 💡 아이디어 / 다음 할 일

- 모달창 디자인 다시 수정
- 일정 추가 페이지(input.html) 레이아웃 구상
- 지역별 수업 관리를 위한 지역 구분 추가

---

## 📅 2026-03-23 개발 내용

### ✅ 완료된 작업

- 달력 표기 부분 수정
    - 해당 월의 마지막 날짜까지만 출력되도록 함
    - 달력에 일정 출력 시 첫번째, 두번째 줄에 표시될 내용 변경
    - 달력 한 칸을 넘어가는 일정은 잘려서 옆 칸에 겹쳐서 보이지 않도록 수정
    - 달력 내 일정 글자 크기 수정
- 페이지 확장성을 위해 구글 스프레드시트 안의 시트명으로 데이터를 불러오도록 수정
    - GAS 코드에서 내가 설정한 시트명으로 data 를 불러올 수 있도록 수정
- 일정 수정 모달창 디자인 수정
    - 디자인이 더 구려짐.. 다시 수정 필요함

### 🐛 해결 중인 버그

- 달력에 일정 출력 내용 변경 시 날짜가 1899-12-30T 형식으로 출력 됨.
    - 개발 전 코드로 돌아가서 다시 코드 수정함. (notebook lm 은 코딩 능력은 안 좋음)

### 💡 아이디어 / 다음 할 일

- 모달창 디자인 다시 수정
- 일정 추가 페이지(input.html) 레이아웃 구상
- 지역별 수업 관리를 위한 지역 구분 추가

---

## 📅 2026-03-22 개발 내용

### ✅ 완료된 작업

- Node.js 설치 및 npx repomix 환경 설정 완료
- .gitignore 추가하여 불필요한 파일 제외

### 🐛 해결 중인 버그

- 일정 수정 시 중복 데이터가 쌓이는 문제 (현재 NotebookLM 분석 중)

### 💡 아이디어 / 다음 할 일

- 일정 추가 페이지(input.html) 레이아웃 구상
- 지역별 수업 관리를 위한 지역 구분 추가
